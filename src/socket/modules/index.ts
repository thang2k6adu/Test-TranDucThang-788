import type { SocketModule } from "./types";

/** Register socket modules here when adding real-time features (Phase 5). */
export const SOCKET_MODULES: SocketModule[] = [];

export type { SocketModule } from "./types";
